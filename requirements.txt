# --- Парсеры документов и конфиг (доменная логика, без изменений) ---
pydantic_settings
pandas
pytest
httpx
python-multipart
openpyxl
python-docx
pdfplumber

# --- Веб ---
fastapi
uvicorn[standard]
gunicorn
jinja2
itsdangerous          # подпись cookie-сессии (SessionMiddleware)

# --- База метаданных сессий (она же очередь задач) ---
# Файлы лежат на диске сервера, задачи — в таблице sessions:
# ни S3-клиента, ни брокера очередей не требуется.
sqlalchemy[asyncio]>=2.0
asyncpg               # Postgres (compose/прод)
aiosqlite             # SQLite (локальная разработка)

# --- Авторизация Keycloak (OIDC) ---
authlib
